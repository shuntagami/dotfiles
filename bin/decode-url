#!/usr/bin/env node

const encodedUrl = process.argv[2];

if (!encodedUrl) {
  console.log("Please provide an encoded URL as an argument.");
  process.exit(1);
}

console.log(`Decoded URL: ${decodeURIComponent(decodeURIComponent(encodedUrl))}`);
